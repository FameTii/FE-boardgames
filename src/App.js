import './App.css';
const cors = require('cors');

App.use(cors());

function App() {
  return (
    <div className="App">
      <header className="App-header">
        hehe
      </header>
    </div>
  );
}

export default App;
